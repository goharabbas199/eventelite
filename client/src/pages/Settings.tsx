import { Layout } from "@/components/Layout";

export default function Settings() {
  return (
    <Layout title="Settings">
      <div className="text-lg font-semibold">Settings Page (Coming Soon)</div>
    </Layout>
  );
}
